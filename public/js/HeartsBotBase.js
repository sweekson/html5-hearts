
define(function () {
  class HeartsBotBase {
    pass (middleware) {
    }

    expose (middleware) {
    }

    pick (middleware) {
    }

    watch (player, card) {
    }
  }

  return HeartsBotBase;
});